// PLACEHOLDER — será substituído automaticamente por `flutterfire configure`.
//
// Mantido no repositório apenas para o projeto compilar antes de alguém rodar
// a configuração. Não contém nenhuma credencial.
import 'package:firebase_core/firebase_core.dart';

class DefaultFirebaseOptions {
  static FirebaseOptions get currentPlatform => throw UnsupportedError(
        'Firebase ainda não foi configurado neste clone.\n'
        'Rode: flutterfire configure\n'
        'Enquanto isso, o app roda em AuthMode.local.',
      );
}
